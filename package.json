{
  "name": "bs-5-simple-starter",
  "version": "1.0.0",
  "description": "Starter for bootstrap websites",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass:build": "sass --no-source-map scss:css",
    "sass:watch": "sass --no-source-map --watch scss:css",
    "vercel-build": "sass --no-source-map scss:css"
  },
  "author": "Junaid Qureshi",
  "license": "MIT",
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.7.2",
    "bootstrap": "^5.3.3",
    "sass": "^1.63.2"
  }
}
